fun recursive of num is
    var b <- -1;
    if num<0 then
        b <- -1;
    else
       print num;
       b <- num -1;
       recursive of b;
    endif;
    returns b;
endfun;

recursive of 10;


